{
  "builds": [
    {
      "src": "svelte.config.js", 
      "use": "@sveltejs/adapter-static"
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/app.html" }
  ]
}
